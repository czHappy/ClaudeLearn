# 继承和多态 - 课程讲义

> 面向对象编程核心概念
>
> 课程时长：90分钟
>
> 编程语言：C++

---

## 📚 课前准备

- 确保已掌握 C++ 类和对象基础
- 准备 C++ 编译环境（g++ 或 Visual Studio）
- 准备好编程热情！🔥

---

## 第一部分：场景导入（15分钟）

### 🎮 我们要做什么？

想象一下，你正在开发一款 RPG 游戏...

> **需求：** 游戏中需要一个武器系统
>
> - 有各种武器：剑（Sword）、弓（Bow）、魔杖（Staff）
> - 每种武器都要能：显示信息、攻击敌人、升级强化

### 💭 让我们先用"最直接"的方法试试

```cpp
// sword.cpp - 剑类
class Sword {
private:
    string name;
    int damage;
    int level;

public:
    Sword(string n, int dmg) : name(n), damage(dmg), level(1) {}

    void showInfo() {
        cout << "武器：" << name << endl;
        cout << "伤害：" << damage << endl;
        cout << "等级：" << level << endl;
    }

    void upgrade() {
        level++;
        damage += 10;
        cout << name << " 升级到等级 " << level << "！" << endl;
    }

    void attack() {
        cout << "🗡️ 挥动" << name << "，造成 " << damage << " 点斩击伤害！" << endl;
    }
};

// bow.cpp - 弓类
class Bow {
private:
    string name;     // 重复了！
    int damage;      // 重复了！
    int level;       // 重复了！

public:
    Bow(string n, int dmg) : name(n), damage(dmg), level(1) {}

    void showInfo() {    // 整个方法都在重复！
        cout << "武器：" << name << endl;
        cout << "伤害：" << damage << endl;
        cout << "等级：" << level << endl;
    }

    void upgrade() {     // 又是重复！
        level++;
        damage += 10;
        cout << name << " 升级到等级 " << level << "！" << endl;
    }

    void attack() {      // 只有这里不同
        cout << "🏹 拉开" << name << "，造成 " << damage << " 点穿刺伤害！" << endl;
    }
};

// staff.cpp - 魔杖类
class Staff {
    // ... 又是一模一样的重复代码 ...

    void attack() {      // 只有攻击方式不同
        cout << "✨ 挥动" << name << "，造成 " << damage << " 点魔法伤害！" << endl;
    }
};
```

### 🤔 让我们思考一下

看看上面的代码，请回答：

1. **这些类有什么共同点？**
   - 都有相同的属性：name、damage、level
   - 都有相同的方法：showInfo()、upgrade()
   - 只有 attack() 方法的实现不同

2. **如果要新增 50 种武器，会怎样？**
   - 要复制粘贴 50 次代码！
   - 如果要修改 showInfo()，要改 50 个地方！
   - 维护噩梦！😱

3. **有没有更好的办法？**
   - 当然有！这就是**继承**的用武之地！

---

## 第二部分：知识点一 - 继承（25分钟）

### 📖 什么是继承？

> **定义：** 继承是面向对象编程的核心机制，允许子类自动获得父类的属性和方法。

**生活类比：**
- 孩子继承父母的基因（属性）
- 孩子继承家族的姓氏（特征）
- 但孩子也有自己的个性（扩展）

### 🔧 继承的语法

```cpp
// 基类（父类）
class Base {
    // 基类成员
};

// 派生类（子类）
class Derived : public Base {
    // 派生类成员
};
```

### 💡 代码示例1：简单继承

```cpp
#include <iostream>
#include <string>
using namespace std;

// 基类：动物
class Animal {
protected:  // 注意是 protected，子类可以访问
    string name;

public:
    Animal(string n) : name(n) {}

    void eat() {
        cout << name << " 正在吃东西" << endl;
    }

    void sleep() {
        cout << name << " 正在睡觉 Zzz..." << endl;
    }
};

// 派生类：狗
class Dog : public Animal {  // public 继承
public:
    // 构造函数调用父类构造函数
    Dog(string n) : Animal(n) {}
};

int main() {
    Dog myDog("旺财");

    // 狗类自动拥有了动物类的方法！
    myDog.eat();    // 输出：旺财 正在吃东西
    myDog.sleep();  // 输出：旺财 正在睡觉 Zzz...

    return 0;
}
```

**✨ 神奇的事情发生了：**
- Dog 类什么都没写，就拥有了 eat() 和 sleep() 方法！
- 这就是继承的魔力！

### 💡 代码示例2：子类扩展新功能

```cpp
class Dog : public Animal {
public:
    Dog(string n) : Animal(n) {}

    // 狗类特有的方法
    void bark() {
        cout << name << " 汪汪叫！🐕" << endl;
    }

    void wagTail() {
        cout << name << " 摇着尾巴~" << endl;
    }
};

int main() {
    Dog myDog("旺财");

    // 继承来的方法
    myDog.eat();      // ✅ 可以用
    myDog.sleep();    // ✅ 可以用

    // 自己新增的方法
    myDog.bark();     // ✅ 输出：旺财 汪汪叫！🐕
    myDog.wagTail();  // ✅ 输出：旺财 摇着尾巴~

    return 0;
}
```

### 💡 代码示例3：方法重写（Override）

有时候，子类想要修改父类的行为：

```cpp
class Cat : public Animal {
public:
    Cat(string n) : Animal(n) {}

    // 重写父类的 eat 方法
    void eat() {
        cout << name << " 优雅地吃着猫粮 🐱" << endl;
        cout << "（猫咪吃东西就是与众不同）" << endl;
    }
};

int main() {
    Animal* animal = new Animal("动物");
    Dog* dog = new Dog("旺财");
    Cat* cat = new Cat("咪咪");

    animal->eat();  // 输出：动物 正在吃东西
    dog->eat();     // 输出：旺财 正在吃东西
    cat->eat();     // 输出：咪咪 优雅地吃着猫粮 🐱
                    //      （猫咪吃东西就是与众不同）

    delete animal;
    delete dog;
    delete cat;

    return 0;
}
```

### 💡 代码示例4：调用父类方法

```cpp
class Bird : public Animal {
private:
    bool canFly;

public:
    Bird(string n, bool fly) : Animal(n), canFly(fly) {}

    void eat() {
        // 先调用父类的 eat 方法
        Animal::eat();

        // 再添加自己的行为
        cout << "吃完后，" << name << " 整理了一下羽毛 🪶" << endl;

        if (canFly) {
            cout << name << " 准备起飞！" << endl;
        }
    }
};

int main() {
    Bird sparrow("小麻雀", true);
    sparrow.eat();

    // 输出：
    // 小麻雀 正在吃东西
    // 吃完后，小麻雀 整理了一下羽毛 🪶
    // 小麻雀 准备起飞！

    return 0;
}
```

### 🎯 回到武器系统：用继承重构

现在，让我们用继承来优雅地解决之前的问题：

```cpp
// 武器基类
class Weapon {
protected:
    string name;
    int damage;
    int level;

public:
    Weapon(string n, int dmg) : name(n), damage(dmg), level(1) {}

    // 公共方法
    void showInfo() {
        cout << "========== 武器信息 ==========" << endl;
        cout << "名称：" << name << endl;
        cout << "伤害：" << damage << endl;
        cout << "等级：" << level << endl;
        cout << "==============================" << endl;
    }

    void upgrade() {
        level++;
        damage += 10;
        cout << "✨ " << name << " 升级成功！" << endl;
        cout << "等级：" << level << " | 伤害：" << damage << endl;
    }

    // 攻击方法留给子类实现
    void attack() {
        cout << "使用 " << name << " 进行攻击！" << endl;
    }
};

// 剑类
class Sword : public Weapon {
public:
    Sword(string n, int dmg) : Weapon(n, dmg) {}

    // 重写攻击方法
    void attack() {
        cout << "⚔️ 挥动" << name << "横扫千军！造成 " << damage << " 点斩击伤害！" << endl;
    }
};

// 弓类
class Bow : public Weapon {
public:
    Bow(string n, int dmg) : Weapon(n, dmg) {}

    void attack() {
        cout << "🏹 拉满" << name << "一箭穿心！造成 " << damage << " 点穿刺伤害！" << endl;
    }
};

// 魔杖类
class Staff : public Weapon {
public:
    Staff(string n, int dmg) : Weapon(n, dmg) {}

    void attack() {
        cout << "🔮 " << name << "魔力涌动！造成 " << damage << " 点魔法伤害！" << endl;
    }
};
```

**🎉 看看我们实现了什么：**
- ✅ 消除了所有重复代码
- ✅ 公共方法只写一次
- ✅ 每个子类只关注自己的特色
- ✅ 新增武器变得超级简单

---

## 第三部分：知识点二 - 多态（25分钟）

### 🤔 新的问题

现在武器系统工作得很好，但是...

> **新需求：** 游戏角色可以装备任意武器，需要一个统一的攻击系统

我们可能会这样写：

```cpp
void characterAttack(string weaponType, string weaponName, int damage) {
    if (weaponType == "sword") {
        cout << "⚔️ 挥动" << weaponName << "横扫千军！造成 " << damage << " 点斩击伤害！" << endl;
    }
    else if (weaponType == "bow") {
        cout << "🏹 拉满" << weaponName << "一箭穿心！造成 " << damage << " 点穿刺伤害！" << endl;
    }
    else if (weaponType == "staff") {
        cout << "🔮 " << weaponName << "魔力涌动！造成 " << damage << " 点魔法伤害！" << endl;
    }
    // 每新增一种武器，就要加一个 else if...
}
```

**😫 问题又来了：**
- 大量的 if-else 判断
- 新增武器要修改这个函数
- 违反了开闭原则

### 📖 什么是多态？

> **定义：** 多态允许不同的对象对同一消息作出不同的响应。

**生活类比：**
- 同样是"叫"这个动作：
  - 狗会"汪汪"
  - 猫会"喵喵"
  - 鸭会"嘎嘎"
- 同样是"移动"这个动作：
  - 人是走路
  - 鱼是游泳
  - 鸟是飞翔

### 🔑 C++ 中的多态实现

在 C++ 中，多态通过**虚函数（virtual function）**实现：

```cpp
class Base {
public:
    virtual void doSomething() {  // 虚函数
        cout << "Base 版本" << endl;
    }
};

class Derived : public Base {
public:
    void doSomething() override {  // 重写虚函数
        cout << "Derived 版本" << endl;
    }
};
```

### 💡 代码示例1：多态基础

```cpp
#include <iostream>
using namespace std;

class Animal {
public:
    // 虚函数 - 这是关键！
    virtual void speak() {
        cout << "动物发出声音" << endl;
    }

    // 虚析构函数 - 很重要！
    virtual ~Animal() {}
};

class Dog : public Animal {
public:
    void speak() override {  // override 关键字表明是重写
        cout << "🐕 汪汪汪！" << endl;
    }
};

class Cat : public Animal {
public:
    void speak() override {
        cout << "🐱 喵喵喵~" << endl;
    }
};

class Duck : public Animal {
public:
    void speak() override {
        cout << "🦆 嘎嘎嘎！" << endl;
    }
};

// 神奇的函数：接受任何动物
void makeSound(Animal* animal) {
    animal->speak();  // 会调用正确的版本！
}

int main() {
    Dog dog;
    Cat cat;
    Duck duck;

    cout << "动物音乐会开始啦！" << endl;
    cout << "-------------------" << endl;

    makeSound(&dog);   // 输出：🐕 汪汪汪！
    makeSound(&cat);   // 输出：🐱 喵喵喵~
    makeSound(&duck);  // 输出：🦆 嘎嘎嘎！

    return 0;
}
```

**✨ 多态的魔法：**
- 同一个函数 `makeSound()`
- 传入不同的动物
- 自动调用正确的 `speak()` 方法！

### 💡 代码示例2：多态与数组

```cpp
int main() {
    // 创建一个动物园！
    Animal* zoo[5];

    zoo[0] = new Dog();
    zoo[1] = new Cat();
    zoo[2] = new Duck();
    zoo[3] = new Dog();
    zoo[4] = new Cat();

    cout << "🎪 欢迎来到动物园！" << endl;
    cout << "动物们要开始表演了：" << endl;
    cout << "=====================" << endl;

    for(int i = 0; i < 5; i++) {
        cout << "动物 " << (i+1) << ": ";
        zoo[i]->speak();  // 自动调用正确的方法
    }

    // 清理内存
    for(int i = 0; i < 5; i++) {
        delete zoo[i];
    }

    return 0;
}
```

### 💡 代码示例3：纯虚函数与抽象类

```cpp
class Shape {  // 抽象类
public:
    // 纯虚函数 - 没有实现
    virtual double getArea() = 0;
    virtual void draw() = 0;

    // 虚析构函数
    virtual ~Shape() {}
};

class Circle : public Shape {
private:
    double radius;

public:
    Circle(double r) : radius(r) {}

    double getArea() override {
        return 3.14159 * radius * radius;
    }

    void draw() override {
        cout << "○ 画了一个半径为 " << radius << " 的圆" << endl;
    }
};

class Rectangle : public Shape {
private:
    double width, height;

public:
    Rectangle(double w, double h) : width(w), height(h) {}

    double getArea() override {
        return width * height;
    }

    void draw() override {
        cout << "□ 画了一个 " << width << "x" << height << " 的矩形" << endl;
    }
};

// 统一处理所有形状
void processShape(Shape* shape) {
    shape->draw();
    cout << "   面积：" << shape->getArea() << endl;
}

int main() {
    Circle circle(5);
    Rectangle rect(4, 6);

    processShape(&circle);
    processShape(&rect);

    return 0;
}
```

### 🎯 回到武器系统：用多态解决问题

```cpp
class Weapon {
protected:
    string name;
    int damage;
    int level;

public:
    Weapon(string n, int dmg) : name(n), damage(dmg), level(1) {}

    // 虚函数！
    virtual void attack() {
        cout << "使用 " << name << " 进行攻击！" << endl;
    }

    virtual ~Weapon() {}  // 虚析构函数
};

class Sword : public Weapon {
public:
    Sword(string n, int dmg) : Weapon(n, dmg) {}

    void attack() override {
        cout << "⚔️ 挥动" << name << "横扫千军！造成 " << damage << " 点斩击伤害！" << endl;
    }
};

class Bow : public Weapon {
public:
    Bow(string n, int dmg) : Weapon(n, dmg) {}

    void attack() override {
        cout << "🏹 拉满" << name << "一箭穿心！造成 " << damage << " 点穿刺伤害！" << endl;
    }
};

// 现在，角色攻击系统变得超级简单！
class Character {
private:
    string name;
    Weapon* weapon;

public:
    Character(string n) : name(n), weapon(nullptr) {}

    void equipWeapon(Weapon* w) {
        weapon = w;
        cout << name << " 装备了武器！" << endl;
    }

    void attack() {
        if (weapon) {
            cout << name << " 发起攻击：" << endl;
            weapon->attack();  // 多态！会调用正确的攻击方法
        } else {
            cout << name << " 手无寸铁，使用拳头攻击！" << endl;
        }
    }
};

int main() {
    // 创建角色
    Character hero("勇者");

    // 创建武器
    Sword excalibur("王者之剑", 100);
    Bow windBow("疾风之弓", 80);

    // 测试不同武器
    hero.equipWeapon(&excalibur);
    hero.attack();

    cout << endl;

    hero.equipWeapon(&windBow);
    hero.attack();

    return 0;
}
```

### 🌟 多态的优势

1. **代码简洁**：不需要 if-else 判断
2. **易于扩展**：新增武器不需要修改现有代码
3. **符合开闭原则**：对扩展开放，对修改关闭

---

## 第四部分：完整武器系统展示（10分钟）

让我们看看最终的武器系统是什么样的：

```cpp
#include <iostream>
#include <vector>
#include <string>
using namespace std;

// 武器基类
class Weapon {
protected:
    string name;
    int damage;
    int level;
    string rarity;  // 稀有度

public:
    Weapon(string n, int dmg, string r)
        : name(n), damage(dmg), level(1), rarity(r) {}

    virtual void attack() = 0;  // 纯虚函数

    virtual void specialSkill() {
        cout << name << " 没有特殊技能" << endl;
    }

    void showInfo() {
        cout << "\n╔════════════════════════╗" << endl;
        cout << "║     " << rarity << " 武器     ║" << endl;
        cout << "╠════════════════════════╣" << endl;
        cout << "║ 名称: " << name << endl;
        cout << "║ 伤害: " << damage << endl;
        cout << "║ 等级: Lv." << level << endl;
        cout << "╚════════════════════════╝" << endl;
    }

    void upgrade() {
        level++;
        damage = damage * 1.2;  // 伤害提升20%
        cout << "\n🌟 强化成功！" << endl;
        cout << name << " → Lv." << level << endl;
        cout << "攻击力提升至 " << damage << "！" << endl;
    }

    virtual ~Weapon() {}
};

// 各种武器实现
class Sword : public Weapon {
public:
    Sword(string n, int dmg) : Weapon(n, dmg, "⚔️ 近战") {}

    void attack() override {
        cout << "\n⚔️ [剑技·破空斩]" << endl;
        cout << "挥动" << name << "划出剑气，造成 " << damage << " 点斩击伤害！" << endl;
    }

    void specialSkill() override {
        cout << "\n💥 [奥义·天翔龙闪]" << endl;
        cout << "释放剑之奥义，造成 " << damage * 3 << " 点真实伤害！" << endl;
    }
};

class Bow : public Weapon {
private:
    int arrowCount = 10;

public:
    Bow(string n, int dmg) : Weapon(n, dmg, "🏹 远程") {}

    void attack() override {
        if (arrowCount > 0) {
            cout << "\n🏹 [精准射击]" << endl;
            cout << "拉满" << name << "，箭矢破空而出！造成 " << damage << " 点穿刺伤害！" << endl;
            arrowCount--;
            cout << "剩余箭矢: " << arrowCount << "/10" << endl;
        } else {
            cout << "\n❌ 箭矢耗尽！需要补充箭矢！" << endl;
        }
    }

    void specialSkill() override {
        cout << "\n🌟 [箭雨风暴]" << endl;
        cout << "天空降下箭雨，造成 " << damage * 2 << " 点范围伤害！" << endl;
        arrowCount = 10;  // 技能后补满箭矢
        cout << "箭矢已补满！" << endl;
    }
};

class Staff : public Weapon {
private:
    int mana = 100;

public:
    Staff(string n, int dmg) : Weapon(n, dmg, "✨ 魔法") {}

    void attack() override {
        if (mana >= 20) {
            cout << "\n✨ [元素冲击]" << endl;
            cout << name << "凝聚魔力，释放元素之力！造成 " << damage << " 点魔法伤害！" << endl;
            mana -= 20;
            cout << "剩余魔力: " << mana << "/100 MP" << endl;
        } else {
            cout << "\n😵 魔力不足！使用法杖敲击，造成 " << damage/3 << " 点物理伤害..." << endl;
            mana += 10;  // 恢复少量魔力
        }
    }

    void specialSkill() override {
        cout << "\n🌈 [禁咒·天地异变]" << endl;
        cout << "召唤远古魔法，造成 " << damage * 4 << " 点毁灭性伤害！" << endl;
        mana = 100;  // 大招后魔力全满
        cout << "魔力已恢复！" << endl;
    }
};

// 游戏系统
class GameSystem {
public:
    void weaponShowcase() {
        cout << "\n═══════════════════════════════" << endl;
        cout << "     欢迎来到武器展示厅！" << endl;
        cout << "═══════════════════════════════" << endl;

        // 创建武器
        vector<Weapon*> weapons;
        weapons.push_back(new Sword("霜之哀伤", 120));
        weapons.push_back(new Bow("风之追忆", 90));
        weapons.push_back(new Staff("贤者之杖", 110));

        // 展示所有武器
        for (auto weapon : weapons) {
            weapon->showInfo();
            weapon->attack();
            weapon->specialSkill();
            cout << "\n----------------------------" << endl;
        }

        // 清理内存
        for (auto weapon : weapons) {
            delete weapon;
        }
    }

    void battleDemo() {
        cout << "\n╔═══════════════════════════╗" << endl;
        cout << "║      战 斗 演 示          ║" << endl;
        cout << "╚═══════════════════════════╝" << endl;

        Character hero("亚瑟王");

        Sword* sword = new Sword("誓约胜利之剑", 150);
        Bow* bow = new Bow("太阳神之弓", 100);

        // 切换武器战斗
        cout << "\n第一回合：近战" << endl;
        hero.equipWeapon(sword);
        hero.attack();

        cout << "\n第二回合：远程" << endl;
        hero.equipWeapon(bow);
        hero.attack();

        delete sword;
        delete bow;
    }
};
```

---

## 第五部分：综合实践预告（5分钟）

### 🎮 接下来的挑战

现在你已经掌握了继承和多态，是时候接受真正的挑战了！

> **项目：魔法学院战斗竞技场**
>
> 在霍格沃兹魔法学院，一年一度的竞技大赛即将开始！

**你将要实现：**

1. **四大学派角色**
   - 🗡️ 战士：狂暴系统，怒气爆发
   - 🔮 法师：魔法系统，元素掌控
   - 🏹 游侠：箭矢系统，精准打击
   - 🗡️ 刺客：暗影系统，致命一击

2. **战斗系统**
   - 回合制战斗
   - 华丽的技能特效
   - 生命值进度条显示

3. **充分运用知识点**
   - 继承：所有角色继承自 Character 基类
   - 多态：统一的战斗接口，不同的攻击实现
   - 虚函数：动态绑定战斗行为

**准备好了吗？让我们开始吧！** 🚀

---

## 📝 知识点总结

### 继承（Inheritance）

✅ **基本语法**
```cpp
class Derived : public Base { };
```

✅ **构造函数调用**
```cpp
Derived() : Base() { }
```

✅ **访问控制**
- `public`: 公开继承
- `protected`: 子类可访问
- `private`: 子类不可访问

✅ **方法重写**
- 子类可以重新定义父类的方法

### 多态（Polymorphism）

✅ **虚函数**
```cpp
virtual void func() { }
```

✅ **纯虚函数（抽象类）**
```cpp
virtual void func() = 0;
```

✅ **虚析构函数**
```cpp
virtual ~Base() { }
```

✅ **override 关键字**
```cpp
void func() override { }
```

✅ **多态的条件**
1. 继承关系
2. 虚函数
3. 基类指针或引用

---

## 🎯 课后练习

1. **思考题**
   - 什么时候应该使用继承？
   - 为什么需要虚析构函数？
   - 继承和组合有什么区别？

2. **编程题**
   - 实现一个交通工具系统（汽车、飞机、轮船）
   - 设计一个图形库（圆形、矩形、三角形）
   - 完成魔法学院战斗系统

---

## 🔥 下节预告

下一节课，我们将学习：
- 运算符重载
- 模板编程
- STL 容器

准备好继续你的 C++ 之旅了吗？

---

> 💡 **记住：** 编程是一门手艺，多写代码才能掌握精髓！
>
> Happy Coding! 🚀