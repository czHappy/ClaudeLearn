## Labs

### 条件与循环
- if-else 语句与嵌套条件
- switch-case 语句及其应用场景
- for 循环（传统形式、范围for循环）
- while 与 do-while 循环的区别
- break、continue 与循环控制
- 循环嵌套与复杂度分析
- 条件运算符（三元运算符）

### 变量及其精度
- 基本数据类型（int, float, double, char, bool）
- 整型的不同宽度（short, int, long, long long）
- 有符号与无符号类型（signed vs unsigned）
- 浮点数精度问题与比较陷阱
- 类型转换（隐式转换、显式转换）
- 数值溢出与未定义行为
- sizeof 运算符与类型大小

### 函数
- 函数声明与定义
- 函数原型与头文件
- 返回值类型
- 函数签名
- 函数重载
- 默认参数
- 内联函数

### 作用域与命名空间
- 局部作用域与全局作用域
- 块作用域与变量生命周期
- 静态局部变量（static）
- namespace 定义与使用
- using 声明与 using 指令
- 作用域解析运算符（::）
- 作用域就近原则


### 内存与指针
- 指针的定义与初始化
- 指针运算（算术运算、比较运算）
- 指针与数组的关系
- 多级指针（指针的指针）
- 动态内存分配（new/delete, new[]/delete[]）
- 内存泄漏与悬空指针
- const 与指针（指向常量的指针、常量指针）


### 参数传递
- 值传递（pass by value）
- 引用传递（pass by reference）
- 指针传递（pass by pointer）
- 数组作为参数传递
- 指针与引用的区别

### 结构体
- struct 定义与初始化
- 成员访问（. 与 ->）
- 结构体数组
- 结构体嵌套
- 结构体与函数（传参与返回）
- 内存对齐与 sizeof
- const 引用传递结构体参数

### 自定义排序
- std::sort 基本用法
- 自定义比较函数
- Lambda 表达式作为比较函数
- 多关键字排序
- 自定义类型的排序

### 单链表
- 节点结构定义
- 链表的创建与初始化
- 头插法与尾插法
- 链表遍历
- 节点查找
- 节点插入（头部、尾部、中间）
- 节点删除
- 链表反转
- 内存管理与内存泄漏防范

### 二进制运算
- 二进制表示与进制转换
- 位运算符（&, |, ^, ~, <<, >>）
- 位掩码（bit mask）技术
- 位运算实现加减乘除
- 位运算常见技巧（判断奇偶、交换变量、2的幂次）
- 位域（bit field）
- 整数的补码表示
- 位运算在权限控制中的应用

### I/O重定向
- 标准输入输出流（cin, cout, cerr）
- 文件流（ifstream, ofstream, fstream）
- 文件打开模式（in, out, app, binary 等）
- 流状态检测（eof, fail, bad, good）
- 格式化输出（setw, setprecision, fixed）
- getline 与字符串输入
- 二进制文件读写
- 字符串流（stringstream）

### 类和对象
- 类的定义与成员访问控制（public, private, protected）
- 构造函数与析构函数
- 拷贝构造函数
- 成员初始化列表
- this 指针
- const 成员函数
- static 成员变量与成员函数
- 友元函数与友元类
- 对象的生命周期

### 运算符重载
- 运算符重载的基本语法
- 成员函数重载 vs 非成员函数重载
- 算术运算符重载（+, -, *, /）
- 关系运算符重载（==, <, > 等）
- 赋值运算符重载（=）
- 下标运算符重载（[]）
- 自增自减运算符重载（前置与后置）
- 流运算符重载（<<, >>）
- 函数调用运算符重载（()）

### STL应用
- 容器概述（序列容器、关联容器、无序容器）
- vector 的使用与内存管理
- list 与 deque
- set 与 map
- unordered_set 与 unordered_map
- 迭代器的类型与使用
- 常用算法（find, count, transform, accumulate）
- 算法与迭代器的配合

### STL String类实现
- 类的基本框架设计
- 动态内存管理（new/delete）
- 深拷贝与浅拷贝
- 拷贝构造函数实现
- 赋值运算符实现（考虑自赋值）
- 常用成员函数实现（size, c_str, append, substr）
- 运算符重载（+, +=, [], ==, <<）
- 输出流重载

