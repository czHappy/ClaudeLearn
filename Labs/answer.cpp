/*
 * 数字梯形 - 参考答案
 * 文件：answer.cpp
 *
 * 此文件供出题者参考，请勿提供给学生
 */

#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    // 外层循环：控制行数，i 表示当前是第几行
    for (int i = 1; i <= n; i++) {
        // 内层循环：输出第 i 行的数字 1 到 i
        for (int j = 1; j <= i; j++) {
            cout << j;
            // 如果不是最后一个数字，输出空格
            if (j < i) {
                cout << " ";
            }
        }
        // 每行结束后换行
        cout << endl;
    }

    return 0;
}

/*
 * 解题思路说明：
 *
 * 1. 观察规律：
 *    - 第 1 行：输出 1 个数字 (1)
 *    - 第 2 行：输出 2 个数字 (1, 2)
 *    - 第 i 行：输出 i 个数字 (1, 2, ..., i)
 *
 * 2. 设计循环：
 *    - 外层循环 i 从 1 到 n，控制行数
 *    - 内层循环 j 从 1 到 i，输出每行的数字
 *
 * 3. 输出细节：
 *    - 数字之间用空格分隔
 *    - 最后一个数字后不加空格（避免格式问题）
 *    - 每行结束要换行
 *
 * 编译运行：
 * g++ answer.cpp -o answer
 * ./answer
 */
